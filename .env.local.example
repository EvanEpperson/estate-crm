# Copy to .env.local and fill in once you pick an email provider.
# Works with any SMTP provider (Resend, SendGrid, Gmail app password, Mailgun, etc.)

SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=resend
SMTP_PASS=your_api_key_here
SMTP_FROM="Your Name <you@yourdomain.com>"
